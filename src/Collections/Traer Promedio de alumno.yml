info:
  name: Traer Promedio de alumno
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/calificacion/promedio/9
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
