info:
  name: Crear Alumno
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/alumno
  body:
    type: json
    data: |-
      {
          "nombre": "Lucas",
          "apellido": "García",
          "dni": "44123456",
          "email": "lucas@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​