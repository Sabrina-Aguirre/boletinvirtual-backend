info:
  name: Crear Calificación
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/calificacion
  body:
    type: json
    data: |-
      {
          "nota": 8,
          "fecha": "2026-04-10",
          "alumno": {
              "id_alumno": 12
          },
          "materia": {
              "id_materia": 1
          }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
