info:
  name: Consultar Materias
  type: http

http:
  method: GET
  url: http://localhost:8080/api/materia
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
