info:
  name: Consultar alumnos
  type: http

http:
  method: GET
  url: http://localhost:8080/api/alumno
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
