info:
  name: Mostrar reporte de alumnos que desaprobaron
  type: http

http:
  method: GET
  url: http://localhost:8080/api/reporte/desaprobados
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
