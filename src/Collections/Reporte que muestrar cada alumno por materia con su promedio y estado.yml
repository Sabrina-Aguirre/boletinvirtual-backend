info:
  name: Reporte que muestrar cada alumno por materia con su promedio y estado
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/reporte/alumnos
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
