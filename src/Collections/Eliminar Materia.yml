info:
  name: Eliminar Materia
  type: http

http:
  method: DELETE
  url: http://localhost:8080/api/materia
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
