info:
  name: Listado de aprobados
  type: http

http:
  method: GET
  url: http://localhost:8080/api/calificacion/aprobados
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
