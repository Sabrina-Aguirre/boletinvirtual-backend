info:
  name: Listado de calificaciones por alumno
  type: http

http:
  method: GET
  url: http://localhost:8080/api/calificacion/alumno/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
